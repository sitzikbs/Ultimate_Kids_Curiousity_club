# Ultimate Kids Curiosity Club - Environment Configuration

# Mock Services Toggle
# Set to False when using real API services
USE_MOCK_SERVICES=True

# API Keys (required only when USE_MOCK_SERVICES=False)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
ELEVENLABS_API_KEY=
GOOGLE_TTS_API_KEY=
FLUX_API_KEY=

# Provider Preferences
LLM_PROVIDER=openai        # Options: openai, anthropic, mock
TTS_PROVIDER=elevenlabs    # Options: elevenlabs, google, openai, mock
IMAGE_PROVIDER=flux        # Options: flux, dalle, mock

# Storage Paths (relative to project root)
DATA_DIR=data
SHOWS_DIR=data/shows
EPISODES_DIR=data/episodes
ASSETS_DIR=data/assets
